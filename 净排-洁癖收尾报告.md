# 净排 · 洁癖收尾完成报告（neat-freak 轻量路径）

> 执行时间：2026-07-21 ｜ 范围：当前项目（不含 node_modules / dist* / .git）
> 原则：先分辨阶段、后整理；**只归档、未删除**，所有物理删除待你确认。

## 影响
- 消除根目录与 `docs/` 的混乱：一次性规划/评审/调试脚本不再散落根目录。
- 修正文档与代码的版本漂移：`README.md` 仍写 `v0.11.0`、结构描述停留在 v0.13.1 拆分前、误引不存在的 `.editorconfig`。
- 修正记忆中的过期引用：`MEMORY.md` 仍引用已落地的 UX 计划并误称设计基座为 `tokens.css`。
- 让下一次会话从唯一现役答案起步，不再被历史规划文档干扰。

## 改动 / 新建

### 1. `README.md`（对齐事实）
- 版本历史描述 `v0.11.0` → `v0.14.1`。
- 项目结构章节重写为真实 `src/`：`plugins/`（headingEnterFix / forceParagraphEnter）、`composables/`、`components/`、`styles/variables.css`；`tests/` 标注 12 个；删除对不存在的 `.editorconfig` 的引用；补充 `docs/archive/` 说明。

### 2. `.workbuddy/memory/MEMORY.md`（规则同步）
- 技术栈新增「设计基座 `src/styles/variables.css`（颜色/字体 CSS 变量），早期文档若写 `tokens.css` 系旧名」。
- 「UX 实施计划」条目更新为**已过期并归档**，指向新路径 `docs/archive/phase-v0.13-ux/UX架构实施计划.md`。

### 3. `docs/` 按阶段归档（仅移动，未删）
| 阶段目录 | 文件数 | 内容 |
|---|---|---|
| `docs/archive/phase-pre-milkdown/` | 3 | Milkdown 采用前的可行性/迁移评估（历史残留） |
| `docs/archive/phase-v0.13-ux/` | 15 | v0.13.x UX/功能规划（含根 `overview.md`、`UX架构重设计-概览.md`） |
| `docs/archive/phase-v0.14-bugfix/` | 10 | v0.14.0 缺陷/审查规划（含根 `FIX_PLAN_v0.14.0.md`、`CODE_REVIEW_v0.14.0.md`） |
| `docs/archive/debug-scripts/` | 3 | `probe_clean.mjs` / `probe_darkmode.mjs` / `probe_verify.mjs` |

### 4. 保留原位（规范常驻，未动）
`docs/STANDARDIZATION.md`、`docs/CODING_STANDARDS.md`、`docs/RELEASE_CHECKLIST.md`、`docs/code-review-standards.md`、`docs/templates/`、`docs/PRD/`。

## 待你确认（已归档，未删除）
- 以上归档文件目前仍在仓库内 `docs/archive/`，**未做物理删除**。若确认无需回溯，可整体删除 `docs/archive/`，或仅删 `debug-scripts/` 与特定阶段。
- `dist-bak-20260715/` 正在 git 删除进行中，本次未动。

## 遗留
- 无未消除 warning；daily log 未动；>30 天日志尚未提炼（最早 07-07 未超 30 天，暂不需）。
- 规范入口与活跃代码未受影响，`vite build` / `vitest` 状态以你最近一次验证（v0.14.1，65 绿）为准。

---
**复核现场仍保留，等待你确认后是否清场。**
