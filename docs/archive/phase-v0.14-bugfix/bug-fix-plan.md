# 净排 · Bug 排除计划

> 基于 2026-07-17 代码审查发现项，按优先级排序。
> 总览：2 项 🔴 已修复，剩余 3 项 🟡 + 3 项 💭。

---

## ✅ 本轮已修复

| 项 | 问题 | 状态 |
|----|------|------|
| 🔴-1 | `aiExpand` 选择文本替换会命中多处匹配（`split().join()` → `replace()`） | ✅ 已修复 |
| 🔴-2 | 快捷键 `onKeydown` 未屏蔽 IME 中文输入状态 | ✅ 已修复 |

---

## 📋 后续排除清单

### 第 1 批：🟡 Suggestion（建议优先处理）

| # | 问题 | 文件位置 | 影响 | 方案简述 | 预估 |
|---|------|---------|------|---------|------|
| 🟡-1 | `buildHtml` 品牌色正则 `color:[^;]+` 在非 hex 颜色值下可能误截断 | `App.vue:L1142` | 低（当前 10 套主题均为 hex，仅新增主题时需注意） | 加注释警告 + 考虑改用 `/\bcolor:\s*(#[0-9a-fA-F]{3,8}\|[a-z]+)/` | 10min |
| 🟡-2 | `themeMode` 默认值 `'light'` 与持久化恢复间有 1–2 帧闪烁 | `App.vue:L599` + `L718-744` | 中（深色模式用户每次刷新会闪一下浅色） | 在模块顶层读取 localStorage，用读取值作为 `ref` 初始值，而非等 `onMounted` | 20min |
| 🟡-3 | 深色/跟随系统 CSS 变量重复定义，后续易漏改 | `App.vue:L2069-2108` | 中（修改一处忘改另一处，bug 隐蔽） | 用 JS 注入统一深色变量集，消除两处硬拷贝；或加醒目注释「两处必须同步修改」 | 30min |
| 🟡-4 | `copyHtml` fallback DOM 元素在异常时可能泄露 | `App.vue:L1678-1699` | 低（极少走到此分支，但存在内存泄露） | fallback 逻辑包 `try/finally` 确保清理 | 15min |
| 🟡-5 | `editorThemeCss` 与 `buildHtml` 的 `themeAccent` 来源不一致 | `App.vue:L1310` vs `L1166` | 低（当前逻辑下不影响实际行为，但概念上易误导） | 统一从品牌覆盖后的 `t` 提取，或统一从 `base` 提取 | 15min |

### 第 2 批：💭 Nit（视时间安排）

| # | 问题 | 文件位置 | 方案简述 | 预估 |
|---|------|---------|---------|------|
| 💭-1 | `THEMES` 数据冗余，23 个标签字段在每个主题重复 | `App.vue:L847-1132` | 提取公共默认值，主题只写差异化字段 | 1h |
| 💭-2 | `importFromText` 每次 `new TurndownService` | `App.vue:L1649` | 模块级缓存单例 | 5min |
| 💭-3 | `.phone-content` 硬编码 `color:#3e3e3e` | `App.vue:L2507` | 加注释说明意图 | 3min |

---

## 🗺 执行顺序建议

```
批次 1（优先）
┌──────────────────────────────────────────┐
│ 🟡-2  themeMode 闪烁修复  ← 最直接影响用户体验 │
│ 🟡-3  CSS 变量去重       ← 维护性收益最大       │
│ 🟡-4  copyHtml 清理兜底  ← 安全兜底            │
│ 🟡-5  themeAccent 统一   ← 防未来 bug          │
│ 🟡-1  正则注释           ← 信息补充             │
└──────────────────────────────────────────┘
批次 2（休闲优化）
┌──────────────────────────────────────────┐
│ 💭-2  Turndown 缓存                      │
│ 💭-3  颜色注释                            │
│ 💭-1  THEMES 重构（体量较大，建议单独迭代）    │
└──────────────────────────────────────────┘
```

---

## 响应策略

- **每项修复**：单独做 → `vite build` 验证 → 更新 CHANGELOG
- **完成后**：`git commit` + 标注对应 Issue
- **暂不动** `buildHtml` 复制/导出管线、`THEMES` 主题数据、AI 业务逻辑（除非已被标记为问题来源）
