# 净排 · 项目 Review 清单

> 日期：2026-07-18
> 基线：v0.13.0-dev, App.vue 3783 行, 新建 3 组件 + 3 composable
> 范围：src/ 全部 .vue/.js 文件, docs/, 构建产物, Git 状态

---

## 🔴 Bug 修复（高优）

| # | 问题 | 所在文件 | 影响 |
|---|------|---------|------|
| B-1 | 上下文工具栏「删除图片」不工作 | `src/App.vue` `deleteCurrentImage()` | 选中图片后无法用工具栏删除 |
| B-2 | 上下文工具栏「调宽度」拖杆不生效 | `src/App.vue` `updateImageWidth()` | 宽度滑块拖动后无反应 |
| B-3 | 插入本地图片后 Ctrl+Z 不工作 | `src/App.vue` `insertImage()` | 撤销历史栈被意外清空 |
| B-4 | `package.json` 版本号仍为 `0.12.0` | `package.json` | 与 CHANGELOG v0.13.0 不一致 |

---

## 🟡 继续拆分（中优）

| # | 模块 | 当前所在 | 建议目标 | 预估行数 |
|---|------|---------|---------|---------|
| S-1 | 装饰面板（`activePanel === 'image'`） | App.vue 70 行 | `src/components/DecorPanel.vue` | +70 / -65 |
| S-2 | 导入面板（`activePanel === 'import'`） | App.vue 30 行 | `src/components/ImportPanel.vue` | +30 / -25 |
| S-3 | 导出面板（`activePanel === 'export'`） | App.vue 50 行 | `src/components/ExportPanel.vue` | +50 / -45 |
| S-4 | 设置面板（`showSettings`） | App.vue 37 行 | `src/components/SettingsPanel.vue` | +40 / -35 |
| S-5 | **CSS 独立**（783 行, 208 个类） | App.vue `<style>` 标签 | `src/styles/main.css` + 各组件 scoped | +783 / -783 |
| S-6 | `useAI.js` composable | App.vue ~70 行 AI 配置逻辑 | `src/composables/useAI.js` | +70 / -65 |
| S-7 | Milkdown 编辑器初始化 | App.vue `onMounted` ~120 行 | `src/composables/useMilkdown.js` | +120 / -115 |

**全部完成后 App.vue 预估：3783 → ~2200 行**

---

## 🟢 质量 / 工程（低优，但长期有收益）

| # | 项 | 现状 | 建议 |
|---|-----|------|------|
| Q-1 | JS bundle 702KB，超过 500KB 警告线 | 无代码分割 | `vite build` 加 `manualChunks`，Milkdown 系拆独立 chunk |
| Q-2 | `dist-bak-20260715/` 目录遗留 | Git 已跟踪（D 状态） | `git rm -r dist*/` 彻底清理 |
| Q-3 | 0 个 TODO/FIXME 标记 + 0 行注释说明坑 | 已知 Bug 靠人脑记 | 在 `insertImage`/`deleteCurrentImage` 旁加 `// FIXME: ...` 注释 |
| Q-4 | 测试覆盖仅 `callAI` + `buildHtml`（2 文件 31 tests） | Onboarding/上下文工具栏/状态栏/AI 推荐均无测试 | 核心功能加集成测试（不要求单元，至少 smoke test） |
| Q-5 | `App.vue` import 19 个模块 | `import THEME_NAMES from themes.js` 等冗余 import 未检查 | 确认 `THEMES` / `THEME_NAMES` 等是否在 App.vue 中仍有引用 |
| Q-6 | Milkdown v7 → 有 v8 版本 | 依赖版本 | 评估升级收益（性能、API稳定性） |
| Q-7 | `CHANGELOG.md` 中「已知问题」节与代码无链接 | 文档与代码脱节 | 在对应函数上加 `// BUG: 见 CHANGELOG v0.13.0 已知问题` |

---

## 📐 架构 / UX 增强

| # | 项 | 现状 | 建议 |
|---|-----|------|------|
| A-1 | 移动端体验未验证 | 有 `.mobile-*` CSS 类但未系统测试 | 在真机/模拟器上跑一遍全流程 |
| A-2 | 图片 base64 导致 Markdown 文本极度膨胀 | 一张手机照片 → 5MB+ base64 字符串 | 加「图片过大提醒压缩」或限制 2MB |
| A-3 | AI 面板推荐规则简单 | 仅靠字数+标题判断 | 后续可用「最近使用频率」优化推荐排序 |
| A-4 | 键盘快捷键帮助浮层 | 已有但无「搜索」入口 | 考虑在底部状态栏加永久 `?` 图标 |
| A-5 | 导出格式仅 HTML | 无 Markdown 导出 | 加「导出 Markdown」按钮（差异化卖点） |

---

## 📊 汇总优先级建议

```
第一梯队（应立即处理）：
  B-1~B-3 (图片操作 Bug)  → 功能损坏，影响核心卖点
  B-4 (版本号)             → 5 秒修完

第二梯队（本周）：
  S-3 (导出面板) + S-4 (设置面板)  → 两个最简单，快速拆
  Q-2 (清理 dist-bak)             → 干净提交
  Q-3 (加 FIXME 注释)             → 防止遗忘

第三梯队（下次迭代）：
  S-5 (CSS 独立)  → 工作量大但收益最高（-783 行）
  S-1/S-2 (装饰/导入面板)
  Q-1 (代码分割)
  A-5 (Markdown 导出)

长期：
  Q-4 (测试)
  Q-5/Q-6 (依赖审计)
  A-1~A-4 (UX 增强)
```
