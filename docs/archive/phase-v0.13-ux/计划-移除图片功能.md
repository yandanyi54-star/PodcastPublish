# 执行计划：移除图片插入/编辑功能

> 日期：2026-07-20
> 原因：图片功能 Bug 密集且根因深（ProseMirror ↔ Markdown 阻抗不匹配），直接移除，用文字提示替代
> 影响：删除 17 个函数 + 4 块 UI + 相关 CSS，App.vue 减少约 300 行
> 来源：`docs/design/影响评估-移除图片功能.md`（审核通过，已补 3 项遗漏）

---

## 删除清单

### UI 块（模板层）— 4 块

| 位置 | 内容 | 行号 |
|------|------|------|
| 装饰面板 | 图片 URL 输入 + 上传按钮 + 拖拽区 + paste-hint | ~346-378 |
| 浮动面板 | 图片编辑面板 `image-edit-controls` | ~446-518 |
| 上下文工具栏 | 图片操作行（宽度拖杆 + 替换 + 删除） | ~513-514 |

### 函数（逻辑层）— 18 个

```
insertImageByUrl       insertImage            insertLocalImage
handleFileSelect       handleImageDrop
openImageEdit          closeImageEdit
matchImageBySrc        replaceImageInMarkdown
commitImageEdit        deleteEditingImage      replaceEditingImage
updateImageWidth       deleteCurrentImage      replaceCurrentImage
getSelectedImageSrc    onImageClick
onDragEnter            onDragLeave
```

### 状态（ref 层）

```
editingImage          editImageAlt          editImageWidth
imageWidthMap         IMAGE_WIDTH_KEY
imageUrl              fileInput             isDragging
MAX_IMAGE_SIZE
```

### 内联代码（非函数删除）

| 位置 | 内容 |
|------|------|
| `loadFromStorage` 内 | 删除 `loadImageWidthMap()` 调用 |
| `renderHtml` | `{ imageWidthMap: imageWidthMap.value }` 改为不传 options |
| 选区轮询计时器（500ms） | 删除 `selectedType.value = 'image'` 的 `else if` 分支 + `imageWidth` 赋值 |
| `onMounted` | 删除 `onImageClick` 事件注册、paste 图片 handler |

### 事件层

| 事件 | 操作 |
|------|------|
| `proseMirror.addEventListener('click', onImageClick)` | 删除 |
| `milkdownContainer?.addEventListener('paste', onEditorPaste)` 图片分支 | 删除 if 分支或整个 handler（视保留的 paste handler 内容而定） |

### CSS

```
.image-upload-*    .image-drop-*    .image-edit-*    .image-notice（替换为纯文字）
.paste-hint         .decor-grid（如果图片按钮是唯一内容）
```

---

## 保留清单

| 内容 | 原因 |
|------|------|
| `buildHtml.js` 全部 | 现有 Markdown 中的 `![alt](url)` 仍正常渲染 |
| `buildHtml` Post-process C（`imageWidthMap`） | 保留为可选参数，不传时跳过，无副作用 |
| 装饰元素（cover/divider/quote） | `insertDecorBlock` 独立路径 |
| 上下文工具栏标题/文字操作 | `selectedType === 'heading'/'text'` 分支独立 |
| 测试文件 | 无图片相关测试，无需修改 |

---

## 执行步骤（逐步验证）

### 步骤 1：模板层删除

```
□ 删除装饰面板中图片 URL 输入行 + 本地上传按钮 + 拖拽区 + paste-hint
  └── 替换为一行提示文案（见下）
□ 删除图片编辑浮动面板（v-if="editingImage" 整个块）
□ 删除上下文工具栏图片行（<template v-if="selectedType === 'image'">）
  └── 构建验证：vite build
```

替换提示文案：
```html
<div class="image-notice">
  <span>📷 如需配图，请直接在 Markdown 中写 <code>![描述](图片链接)</code></span>
</div>
```

### 步骤 2：逻辑层删除

```
□ 删除 18 个图片函数
□ 删除选区轮询中 selectedType = 'image' 分支
  └── 构建验证：vite build
```

### 步骤 3：状态层 + 内联代码清理

```
□ 删除 11 个图片相关 ref / const
□ 删除 loadFromStorage 中的 loadImageWidthMap() 调用
□ renderHtml 参数改为不传 options
□ 删除 imageWidthMap 相关函数（loadImageWidthMap / saveImageWidthMap / cleanImageWidthMap）
  └── 构建验证：vite build
```

### 步骤 4：事件层清理

```
□ 删除 onMounted 中 onImageClick 事件注册
□ 删除 onMounted 中 paste 图片 handler（onEditorPaste 图片分支）
□ 删除 onUnmounted 中 _proseMirrorClick 清理
□ 删除 onUnmounted 中 paste handler 清理
  └── 构建验证：vite build
```

### 步骤 5：CSS 清理

```
□ 删除 .image-upload-* / .image-drop-* / .image-edit-* 相关 CSS
□ 保留 .image-notice 的可视样式（配合新的纯文字提示）
  └── 构建验证：vite build
```

### 步骤 6：最终验证

```
□ vite build → 零 error
□ vitest run → 31/31 pass
□ 手动验证：
  - 装饰元素（封面/分割线/金句）正常插入
  - 上下文工具栏标题/文字操作正常
  - 预览正常渲染
  - F12 控制台无红字报错
```

---

## 确认清单

| # | 项 | 确认 |
|---|-----|------|
| 1 | 移除的是「插图」，不是「装饰元素」 | ⬜ |
| 2 | `buildHtml` 仍正确渲染已有 Markdown 中的图片 | ⬜ |
| 3 | `vite build` 零 error | ⬜ |
| 4 | `vitest run` 31/31 pass | ⬜ |
| 5 | 控制台无引用错误 | ⬜ |
